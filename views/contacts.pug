extends layout.pug

block content 
    h2.center-header Contacts
      ul
        each contact in contacts
          li= `${contact.name} ${contact.lastName}`
          li= ` Tel: ${contact.phone}`
          li= `Email: ${contact.email}`
